# Local development only — copy to .env and fill in.
# Never commit .env

CURSOR_API_KEY=
# Shared secret for Cursor extension ↔ Companion WebSocket (both processes must match)
MIMICA_BRIDGE_TOKEN=
# tutti voice readout — localhost only (127.0.0.1, ::1, or localhost). Non-loopback values are rejected.
# MIMICA_TUTTI_URL=http://127.0.0.1:8787
# MIMICA_VOICE_READOUT_ENABLED=1
